const DEFAULT_SERVER_PORT = 5000

const URL_ID_LENGTH = 10

const NODE_ENV_OPTIONS = {
    DEV: 'development',
    PROD: 'production',
}

module.exports = {
    DEFAULT_SERVER_PORT,
    URL_ID_LENGTH,
    NODE_ENV_OPTIONS
}